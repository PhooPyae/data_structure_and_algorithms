def sort_intervals(intervals):
    #sort the intervals
    #return sorted interval list
    sorted_intervals = sorted(intervals)
    return sorted_intervals

def create_meetingrooms(start_time, heap):
    #create a meeting room heap with end time heapq.heappush(room, endtime)
    #assign the same meeting room if topmost of heap heap[0] <= start time
    #otherwise, create a new room
    return len(meeting_room)

class Solution:
    def minMeetingRooms(self, intervals: List[List[int]]) -> int:
        sorted_intervals = sort_intervals(intervals)
        for i in range(len(intervals)):
            
        #sort the intervals
        #iteration: 
            #create meeting room comparing start times and end times in heap
            #build min_heap tree for end_times corresponding to start time in all intervals

        
        